name: Daily Lumen Update

on:
  schedule:
    - cron: '30 22 * * *'  # 매일 한국 시간 오전 7시 30분 (실제 8시경 실행)
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # ← 이것도 추가!
    
    steps:
    - name: 저장소 코드 가져오기
      uses: actions/checkout@v3
      with:
        fetch-depth: 0  # 전체 히스토리 가져오기
        
    - name: 파이썬 설치하기
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: 필요한 도구 설치하기
      run: |
        pip install feedparser requests python-dotenv google-generativeai
        
    - name: 뉴스 수집 및 사이트 업데이트 실행
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: |
        python main.py
        
    - name: 변경사항 저장하고 올리기
      run: |
        git config --global user.name 'LumenBot'
        git config --global user.email 'bot@lumenmedi.com'
        
        git add -f index.html
        
        if ! git diff --cached --quiet; then
          git commit -m "🚀 Daily Update: $(date +'%Y-%m-%d %H:%M UTC')"
          git pull --rebase origin main || echo "이미 최신 상태"
          git push origin main
          echo "✅ 업데이트 완료!"
        else
          echo "ℹ️ 변경사항 없음"
        fi